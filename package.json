{
  "name": "toko-online",
  "version": "1.0.0",
  "description": "E-commerce platform with Laravel backend and Next.js frontend",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && php artisan serve",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "cd backend && composer install && cd ../frontend && npm install",
    "setup": "npm run install:all && npm run setup:backend",
    "setup:backend": "cd backend && php artisan key:generate && php artisan jwt:secret && php artisan migrate --seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["ecommerce", "laravel", "nextjs", "react", "typescript"],
  "author": "Your Name",
  "license": "MIT"
}